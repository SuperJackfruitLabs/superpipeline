{
  "name": "@superpipeline/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --var DEV_AUTH:true --var ENFORCE_CONTROL_PAIR:false",
    "build:web": "pnpm --filter @superpipeline/web build",
    "deploy": "pnpm build:web && wrangler deploy --var DEV_AUTH:false",
    "db:migrate:local": "wrangler d1 migrations apply superpipeline-catalog --local",
    "db:migrate": "wrangler d1 migrations apply superpipeline-catalog --remote",
    "db:seed:local": "wrangler d1 execute superpipeline-catalog --local --file scripts/seed-dev.sql",
    "dev:setup": "pnpm db:migrate:local && pnpm db:seed:local"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@superpipeline/contract": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "jose": "^6.2.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.18",
    "@cloudflare/workers-types": "^4.20260619.1",
    "@superpipeline/agent-sdk": "workspace:*",
    "wrangler": "^4.103.0"
  }
}
