{
  "//": "Point Claude Code at a running Superpipeline board worker (pnpm --filter @superpipeline/api dev).",
  "//auth": "LOCAL ONLY. Dev bearer '<tenantId>:<agentId>:<caps>' is accepted only when the server runs with DEV_AUTH=true (docs/05 \u00a72).",
  "//usage": "Copy into your project's .mcp.json, then in Claude Code the superpipeline_* tools become available.",
  "mcpServers": {
    "superpipeline": {
      "type": "http",
      "url": "http://localhost:8787/mcp",
      "headers": {
        "Authorization": "Bearer tnt_dev:agt_research:research,publish"
      }
    }
  },
  "//deployed": "Against a deployed board use your agent token instead: \"Authorization\": \"Bearer spa_...\" (minted by Connect an agent). There is NO OAuth flow to run."
}
